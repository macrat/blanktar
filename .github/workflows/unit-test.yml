name: Unit Test

on:
  push:

jobs:
  test:
    name: Unit Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v1
      - uses: actions/checkout@v1
      - run: npm ci
      - run: npm test
